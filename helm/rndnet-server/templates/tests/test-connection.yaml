apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rndnet-server.fullname" . }}-test-connection"
  labels:
    {{- include "rndnet-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rndnet-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
