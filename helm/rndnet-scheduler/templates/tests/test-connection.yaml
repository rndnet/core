apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rndnet-scheduler.fullname" . }}-test-connection"
  labels:
    {{- include "rndnet-scheduler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rndnet-scheduler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
